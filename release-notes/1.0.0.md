## Highlights

- Market launch packaging now supports Developer ID signing, notarization, Sparkle updates, installer output, generated app icons, and release manifests.
- Production release automation adds CI packaging, artifact verification reports, Sparkle appcast promotion, hosted rollback snapshots, and rollback scripting.
- Ask the Assistant defaults to private local mode, with explicit provider-sharing consent before project file excerpts or command output can leave the Mac.
- Editor saves use scoped diffs, undo checkpoints, and secret scanning before writing to disk.
- Launch readiness adds local crash reporting, telemetry controls, commercial license activation, entitlement refresh, account recovery, offline grace, license transfer, onboarding, support bundle export/upload, issue IDs, diagnostic version stamps, log redaction, and crash symbolication metadata.

## Verification

- Swift package tests and smoke tests cover planner routing, scanner behavior, tool command generation, process handling, UI coverage harnesses, editor save safety, provider-sharing gates, and launch readiness diagnostics.

## Support

- Use Session > Diagnostics > Launch Readiness > Support Bundle to export or consent-upload redacted local diagnostics for support.
- Crash reports and telemetry event logs stay local unless the user explicitly exports a support bundle or enables the relevant upload consent.
